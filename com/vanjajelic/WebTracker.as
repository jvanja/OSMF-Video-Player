package  com.vanjajelic{		//IMPORT THE GOOGLE ANALYTICS CLASSES - MUST HAVE ANALYTICS INSTANCE IN .FLA'S LIBRARY	import com.google.analytics.AnalyticsTracker;	import com.google.analytics.GATracker;	import flash.display.DisplayObject;		public class WebTracker {		private static var gATracker:WebTracker;		private static var allowInstantiation:Boolean;		private var theTracker:GATracker;		public function WebTracker() 		{			if (!allowInstantiation) 			{				throw new Error("Error: Instantiation failed: Use SingletonDemo.getInstance() instead of new.");			}		}				public static function getInstance():WebTracker 		{			if (gATracker == null) 			{				allowInstantiation = true;				gATracker = new WebTracker();				allowInstantiation = false;			}			return gATracker;		}				public function setUpGA(display:DisplayObject, accNumber:String, debugging:Boolean = false):void 		{			theTracker=new GATracker( display, accNumber, "AS3", debugging );		}				public function trackPlayerEvent(str:String, str2:String, action:String):void		{			trace("[GA TRRACKING]");			trace("TRACKED: " + str + " " + str2 + " " + action);			theTracker.trackEvent( str, str2 ,action );		}	}	}